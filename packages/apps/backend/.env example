DB_HOST=192.168.1.10
DB_PORT=5432
DB_USER=postgress
DB_PASS=postgress
DB_NAME=EasyCharts

DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin

JWT_SECRET=JWTKEY
JWT_EXPIRE_IN=30m

REFRESH_JWT_SECRET=JWTREFRESHKEY
REFRESH_JWT_EXPIRE_IN=1d

# AI Chat feature
# Set AI_ENABLED=true to enable the chat panel (requires a running Ollama instance)
# Recommended models with tool-call support: qwen2.5:7b, llama3.1:8b, mistral:7b
AI_ENABLED=false
AI_OLLAMA_URL=http://localhost:11434
AI_OLLAMA_MODEL=qwen2.5:7b
